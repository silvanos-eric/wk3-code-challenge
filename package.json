{
  "name": "wk3-code-challenge",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "npx json-server --no-cors --watch db.json"
  },
  "devDependencies": {
    "vite": "^5.3.1"
  },
  "dependencies": {
    "json-server": "0"
  }
}
